name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python>=3.6
  - pip
  # Package dependencies
  - pynacl
  - requests
  - protobuf
  - jupyterlab
    # Testing
  - pytest
  - pytest-cov
  - codecov
  # Docs
  - sphinx
  - sphinx_rtd_theme
  - nbsphinx
  # Formatting
  - black
    # Pip-only installs
  #- pip:
  #  - codecov

